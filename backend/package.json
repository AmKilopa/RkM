{
  "name": "rkm-backend",
  "version": "1.0.0",
  "description": "Backend для RkM проекта",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "install-puppeteer": "set PUPPETEER_SKIP_DOWNLOAD=true && npm install puppeteer"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "axios": "^1.6.0",
    "dotenv": "^16.3.0",
    "bcrypt": "^5.1.0",
    "helmet": "^7.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}